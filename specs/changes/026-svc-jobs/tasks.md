# 026 tasks
- [x] services/svc-jobs（deep 代理交付，逐文件验收 + 本地 build/vet）
- [x] 集成：pf_svc_jobs 建库 + env + initdb + auth register-client svc-jobs + svc-notify accept 白名单
- [x] QA：即时投递 done / 失败退避 dead 路径 / cron 每分钟触发 / 权限 403 / 契约三件套
