# 015 tasks

- [x] proposal
- [ ] `tools/pctl/manifest.go`: first-party + `data.database` ⇒ require `migrations/*.sql` (validation error)
- [ ] `templates/go-service/main.go`: comment pointing at the convention (no pgx dependency added to the bare template)
- [ ] Reference runner `migrate.go` lands with svc-file (016) and is copied by svc-notify (017) / svc-users (018)
- [ ] docs: architecture-v2.md ADR row
